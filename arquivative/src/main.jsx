import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import ArquivativeC from "./ArquivativeC";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ArquivativeC />
  </React.StrictMode>
);